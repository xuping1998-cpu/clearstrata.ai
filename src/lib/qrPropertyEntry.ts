/**
 * 扫码 / 旧自动入楼链：从 `unifiedPropertyEntry` 导出，保持既有 import 路径。
 */
export {
  tryAutoJoinProperty,
  type TryAutoJoinPropertyInput,
  type TryAutoJoinPropertySuccess,
  type TryAutoJoinPropertyFailure,
  type TryAutoJoinPropertyResult,
} from './unifiedPropertyEntry';
