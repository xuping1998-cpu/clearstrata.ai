/**
 * 与 Next.js App Router 的 `app/join/[code]/page.tsx` 对应的路由文件占位：
 * 实际路由在 `App.tsx` 中注册为 `/join/:code`，组件实现见 `../JoinCodeScanPage.tsx`。
 */
export { JoinCodeScanPage as default } from '../JoinCodeScanPage';
