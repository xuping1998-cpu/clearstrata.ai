import { HomeBudgetPanel } from '../dashboard/HomeBudgetPanel';

/** @deprecated Use `HomeBudgetPanel` directly; kept for existing imports (e.g. Dashboard). */
export function BudgetDashboardSection() {
  return <HomeBudgetPanel />;
}
