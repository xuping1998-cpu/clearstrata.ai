# RC-011 IU-3 Evidence Lock — `20261422120000`

| Field | Value |
|-------|-------|
| **Migration version** | `20261422120000` |
| **Migration filename** | `20261422120000_sgm_pause_email_deliveries.sql` |
| **Linked project ID** | `wqohkxtqozscmwfrryfl` |
| **Evidence lock timestamp** | 2026-07-30 (UTC-7) |
| **Classification (IU-2)** | REPAIR_REQUIRED |
| **Lock status** | **Preserved — pre-repair snapshot below; do not overwrite** |

---

## 1. Repository migration intent

Create `sgm_pause_email_deliveries` ledger table with indexes, RLS, staff SELECT policy, and constraints for SGM pause email idempotency.

## 2. Expected catalog objects

| Kind | Objects |
|------|---------|
| Tables | `public.sgm_pause_email_deliveries` |
| Indexes | `idx_sgm_pause_email_deliveries_meeting_user`, `idx_sgm_pause_email_deliveries_property` |
| Constraints | PK; `sgm_pause_email_deliveries_meeting_user_attempt_unique`; status CHECK (`sent`, `failed`); FKs |
| Policies | `sgm_pause_email_deliveries_select_staff` |
| RLS | Enabled |

## 3. Pre-repair live catalog (factual)

| Kind | State |
|------|-------|
| Table | **Present** |
| Indexes | **Present** (both) |
| Constraints | **Present** |
| Policy | **Present** — `sgm_pause_email_deliveries_select_staff` |
| RLS | **Enabled** |

## 4. Missing objects

None at object-existence level.

## 5. Materially different objects

None.

## 6. Statement-collision objects

| Object | Issue |
|--------|-------|
| `sgm_pause_email_deliveries_select_staff` | Bare `CREATE POLICY` in repo; policy exists in catalog |

## 7. Pre-repair policy definition (preserved)

```
USING: property_id IN user_property_ids()
  AND EXISTS (property_members staff role check: council, admin, manager, property_admin)
```

## 8. Evidence queries used

- IU-1 inventory §5.5
- `pg_policy` + `pg_get_expr` on `sgm_pause_email_deliveries`
- `information_schema.tables`, `pg_indexes`, `pg_class.relrowsecurity`

## 9. Pre-repair catalog snapshot

- DB migration head: `20261326120000`
- Policy count on table: **1**
- History row for this version: **Absent**

## 10. Equivalence criteria for IU-4

1. All expected objects present with matching definitions
2. Re-apply of repaired migration SQL succeeds without duplicate-object error
3. Policy count remains **1** (no duplicates)
4. RLS enabled
5. No `schema_migrations` row until IU-4

## 11. Rollback readiness (pre-repair)

| Item | Detail |
|------|--------|
| Objects modified | Migration file only (policy guard); catalog unchanged on successful guard |
| Rollback procedure | Revert migration file to bare `CREATE POLICY`; no catalog change required if guard skipped creation |
| Rollback authority | Engineering lead |
| Data-loss assessment | **None** — repair is additive guard only |
| Transactional | Partial (file has no explicit transaction) |

## 12. Repair executed

| Item | Value |
|------|-------|
| Artifact | `supabase/migrations/20261422120000_sgm_pause_email_deliveries.sql` — guarded `CREATE POLICY` via `DO $$ IF NOT EXISTS (pg_policies)` |
| Execution | `npx supabase db query --linked --file` re-apply — **exit 0** |
| Post-repair policy count | **1** |
| IU-4 eligibility | **ELIGIBLE_FOR_BACKFILL** |
