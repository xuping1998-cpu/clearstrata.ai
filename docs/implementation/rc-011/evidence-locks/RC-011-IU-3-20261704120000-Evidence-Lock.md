# RC-011 IU-3 Evidence Lock — `20261704120000`

| Field | Value |
|-------|-------|
| **Migration version** | `20261704120000` |
| **Migration filename** | `20261704120000_governance_matters.sql` |
| **Linked project ID** | `wqohkxtqozscmwfrryfl` |
| **Evidence lock timestamp** | 2026-07-30 (UTC-7) |
| **Classification (IU-2)** | REPAIR_REQUIRED |
| **Lock status** | **Preserved — pre-repair snapshot below** |

---

## 1. Repository migration intent

Governance matter domain: four tables, revision/comment triggers, six functions, seven RLS policies, grants.

## 2. Expected catalog objects

Tables: `governance_matters`, `governance_matter_revisions`, `governance_matter_comments`, `governance_matter_comment_moderation`. Five indexes. Six functions. Four triggers (with `DROP TRIGGER IF EXISTS` in repo). Seven policies. RLS on all four tables.

## 3. Pre-repair live catalog

All tables, indexes, functions, triggers, policies, grants **present**. RLS **enabled** on all four tables.

## 4. Missing objects

None.

## 5. Materially different objects

None.

## 6. Statement-collision objects

Seven policies with bare `CREATE POLICY`: `gm_select_tenant`, `gm_insert_council`, `gm_update_council`, `gm_rev_select_tenant`, `gm_comment_select_tenant`, `gm_comment_insert_member`, `gm_mod_select_staff`.

**Note:** Triggers already use `DROP TRIGGER IF EXISTS` in repository — no trigger collision on re-apply.

## 7. Pre-repair policy counts (preserved)

| Table | Policies |
|-------|----------|
| `governance_matters` | 3 |
| `governance_matter_revisions` | 1 |
| `governance_matter_comments` | 2 |
| `governance_matter_comment_moderation` | 1 |

## 8. Evidence queries used

- IU-1 inventory §5.7
- `pg_policy`, `pg_trigger`, `pg_proc`, `information_schema.tables`
- `has_function_privilege('authenticated', 'moderate_governance_matter_comment(...)')` → true

## 9. Pre-repair catalog snapshot

- DB head: `20261326120000`
- History row: **Absent**
- Total policies in scope: **7**

## 10. Equivalence criteria for IU-4

Re-apply succeeds; policy counts unchanged; functions SECURITY DEFINER attributes preserved; no duplicate policies.

## 11. Rollback readiness

| Item | Detail |
|------|--------|
| Objects modified | Migration file (policy guards); `CREATE OR REPLACE FUNCTION` on re-apply refreshes function bodies identically |
| Rollback | Revert file; optional function body compare if concerned |
| Data-loss | **None** |
| Transactional | **Yes** (`BEGIN`/`COMMIT`) |

## 12. Repair executed

| Item | Value |
|------|-------|
| Artifact | `supabase/migrations/20261704120000_governance_matters.sql` — seven guarded policies |
| Execution | Re-apply **exit 0** |
| Post-repair policy counts | 3+1+2+1 = **7** (unchanged) |
| IU-4 eligibility | **ELIGIBLE_FOR_BACKFILL** |
