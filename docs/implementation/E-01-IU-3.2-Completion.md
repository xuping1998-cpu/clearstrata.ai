# E-01-IU-3.2 — Completion Record

| Field | Value |
|-------|-------|
| **Task** | E-01 Snapshot Foundation |
| **Phase** | Phase 3 — Resolution Snapshot Foundation |
| **Implementation Unit** | **IU-3.2** — Resolution Snapshot Immutability |
| **Status** | **Completed** |
| **Completed** | 2026-07-29 |
| **Production effect** | Migration `20261728120000` applied on linked DB via RC-011 IU-5 (2026-07-31) |

---

## Authoritative Source

| Field | Value |
|-------|-------|
| **Implementation Plan** | [`E-01-Implementation-Plan.md`](E-01-Implementation-Plan.md) |
| **Revision** | v1.0 |
| **Verified** | **YES** |

---

## 1. Implementation Unit

| Field | Value |
|-------|-------|
| **Engineering task** | E-01 Snapshot Foundation |
| **Phase** | Phase 3 — Resolution Snapshot Foundation |
| **IU** | **IU-3.2** |
| **Title** | Resolution Snapshot Immutability |

---

## 2. Status

**Completed**

---

## 3. Objective

Implement persistence-layer UPDATE and DELETE protection for `owner_vote_resolution_snapshot` and `owner_vote_frozen_motions` rows correlated to a Freeze Event (`freeze_event_id IS NOT NULL`), per Blueprint §9 / INV-1. Mirrors the IU-2.2 voter snapshot immutability pattern. No population, ballot binding, orchestration, or application changes.

---

## 4. Files Modified

| File | Change |
|------|--------|
| `supabase/migrations/20261728120000_e01_iu32_resolution_snapshot_immutability.sql` | Added |
| `docs/implementation/E-01-IU-3.2-Completion.md` | Added |

---

## 5. Database Changes

- **Migration:** `20261728120000_e01_iu32_resolution_snapshot_immutability.sql`
- **Functions:**
  - `public.owner_vote_resolution_snapshot_event_linked_immutable()` (new)
  - `public.owner_vote_frozen_motions_event_linked_immutable()` (new)
- **Triggers:**
  - `trg_owner_vote_resolution_snapshot_event_linked_immutable` on `public.owner_vote_resolution_snapshot`
  - `trg_owner_vote_frozen_motions_event_linked_immutable` on `public.owner_vote_frozen_motions`
- **Tables / columns:** No schema shape changes; table comments updated
- **Mechanism:** `BEFORE UPDATE OR DELETE` row triggers with `WHEN (OLD.freeze_event_id IS NOT NULL)`

### Trigger summary

| Object | Table | Behavior |
|--------|-------|----------|
| `owner_vote_resolution_snapshot_event_linked_immutable()` | `owner_vote_resolution_snapshot` | Raises on UPDATE/DELETE when invoked |
| `trg_owner_vote_resolution_snapshot_event_linked_immutable` | `owner_vote_resolution_snapshot` | Fires when `OLD.freeze_event_id IS NOT NULL` |
| `owner_vote_frozen_motions_event_linked_immutable()` | `owner_vote_frozen_motions` | Raises on UPDATE/DELETE when invoked |
| `trg_owner_vote_frozen_motions_event_linked_immutable` | `owner_vote_frozen_motions` | Fires when `OLD.freeze_event_id IS NOT NULL` |

**Not blocked:** INSERT on both tables (E-02 population path).

---

## 6. Application Changes

**No application behavior changed.**

No RPC, React, Edge Function, scheduler, or repository modifications.

---

## 7. Verification

| Check | Result |
|-------|--------|
| **Build** | Pass (`npm run build`) |
| **Typecheck** | Not run; no application files modified |
| **Migration apply** | Applied on linked DB `wqohkxtqozscmwfrryfl` via RC-011 IU-5 (2026-07-31) |
| **UPDATE rejection (resolution snapshot)** | ✓ **Passed** — Test D ([`rc-011/iu-5-immutability-negative-tests.sql`](rc-011/iu-5-immutability-negative-tests.sql)) |
| **DELETE rejection (resolution snapshot)** | ✓ **Passed** — Test E |
| **UPDATE rejection (frozen motions)** | ✓ **Passed** — Test F |
| **DELETE rejection (frozen motions)** | ✓ **Passed** — Test G |
| **Regression** | □ Pending — Phase 5 |
| **Runtime behavior** | Unchanged by design; SQL smoke only (RC-011 IU-5) |

### Staging verification SQL (run after migration apply)

```sql
-- Requires fixture freeze event and resolution snapshot from E-02 or test seed.

-- 1) Resolution snapshot: UPDATE must fail
UPDATE owner_vote_resolution_snapshot
SET frozen_at = now()
WHERE freeze_event_id = :fe_id;
-- Expected: ERROR — Event-linked owner_vote_resolution_snapshot rows are immutable (E-01 INV-1)

-- 2) Resolution snapshot: DELETE must fail
DELETE FROM owner_vote_resolution_snapshot
WHERE freeze_event_id = :fe_id;
-- Expected: ERROR — Event-linked owner_vote_resolution_snapshot rows cannot be deleted (E-01 INV-1)

-- 3) Frozen motion: UPDATE must fail
UPDATE owner_vote_frozen_motions
SET title = 'changed'
WHERE freeze_event_id = :fe_id;
-- Expected: ERROR — Event-linked owner_vote_frozen_motions rows are immutable (E-01 INV-1)

-- 4) Frozen motion: DELETE must fail
DELETE FROM owner_vote_frozen_motions
WHERE freeze_event_id = :fe_id;
-- Expected: ERROR — Event-linked owner_vote_frozen_motions rows cannot be deleted (E-01 INV-1)
```

---

## 8. Verification Status

*Records current verification state only (CES-010 DOC-10, [§11](CES-010-Documentation-and-Knowledge-Engineering-Standard.md#11-verification-status-v12)). Each gate **shall** use exactly one of: **✓ Passed**, **□ Pending**, or **N/A**. **N/A** must not bypass required verification.*

| Gate | Status | Evidence |
|------|--------|----------|
| **Design Review** | ✓ **Passed** | Scope aligned to Implementation Plan v1.0 and Blueprint §9; Authoritative Source Verified **YES** |
| **Implementation Review** | ✓ **Passed** | Migration reviewed — IU-2.2 trigger pattern mirrored; additive scope; stop condition respected |
| **Build Verification** | ✓ **Passed** | `npm run build` succeeded in IU-3.2 session |
| **Database Verification** | ✓ **Passed** | RC-011 IU-5: migration applied; immutability tests D–G passed on linked DB |
| **Runtime Verification** | ✓ **Passed** | SQL smoke checks in RC-011 IU-5; no browser regression |
| **Regression Verification** | □ **Pending** | Phase 5 verification |

---

## 9. Backward Compatibility

Fully preserved for current production. Resolution snapshot and frozen motion tables are empty until E-02 orchestration populates them. No changes to `freeze_owner_vote_snapshot`, `submit_owner_vote`, `submit_owner_election_ballot`, live `owner_vote_resolutions`, or `owner_vote_ballots`. Voter snapshot legacy path (NULL `freeze_event_id`) remains governed by IU-2.2.

---

## 10. Resolution Snapshot Status

| Area | Status |
|------|--------|
| **Schema** | ✓ Complete |
| **Immutability** | ✓ Complete |
| **Population** | □ Not Started |
| **Repository** | □ Not Started |
| **Runtime Integration** | □ Not Started |

---

## 11. Invariant Coverage

*Informational only — cumulative E-01 immutability guarantees through IU-3.2. Uses existing Blueprint invariant **INV-1** (snapshot immutability post-freeze) where applicable; no new invariant identifiers introduced.*

### Implemented (persistence layer)

| Guarantee | Phase / IU | Mechanism | Blueprint anchor |
|-----------|------------|-----------|------------------|
| ✓ **Event-linked Voter Snapshot Immutability** | Phase 2 — IU-2.2 | `trg_owner_vote_voter_snapshot_event_linked_immutable` on `owner_vote_voter_snapshot` when `OLD.freeze_event_id IS NOT NULL` | INV-1 (partial — event-linked rows only) |
| ✓ **Resolution Snapshot Immutability** | Phase 3 — IU-3.2 | `trg_owner_vote_resolution_snapshot_event_linked_immutable` on `owner_vote_resolution_snapshot` when `OLD.freeze_event_id IS NOT NULL` | INV-1 (partial — instrument header) |
| ✓ **Frozen Motion Immutability** | Phase 3 — IU-3.2 | `trg_owner_vote_frozen_motions_event_linked_immutable` on `owner_vote_frozen_motions` when `OLD.freeze_event_id IS NOT NULL` | INV-1 (partial — frozen motion content) |

**Scope note:** Hooks enforce UPDATE/DELETE rejection at the database layer only. Full INV-1 satisfaction additionally requires E-02 population and downstream verification (Phase 5). Legacy voter snapshot rows with `freeze_event_id IS NULL` remain outside immutability hooks until E-02 linkage.

### Future (not implemented in E-01 through IU-3.2)

| Guarantee | Status | Owning work |
|-----------|--------|-------------|
| □ **Ballot Frozen Identity** | Not started | E-03 — ballot binding to frozen motion identity (CITM 11) |
| □ **Repository Read Consistency** | Not started | Phase 4 — typed read layer; fail-closed dual-snapshot loaders |
| □ **End-to-End Freeze Consistency** | Not started | E-02 — atomic freeze orchestration (Blueprint INV-4); E-02 + E-03 + Phase 5 verification |

---

## 12. Known Limitations

- No resolution snapshot or frozen motion rows populated — E-02 required
- No ballot frozen-motion identity column — deferred (E-03 / remaining Phase 3 scope per Implementation Plan)
- Linked DB apply complete (RC-011 IU-5); broader Phase 5 regression still pending

---

## 13. Deferred Work

| Next | Description |
|------|-------------|
| **Phase 4** | Typed read repository — dual-snapshot loaders |
| **E-02** | Freeze orchestration — populate resolution snapshot + motions on commit |
| **E-03** | Ballot binding to frozen motion identity |
| **Phase 5** | E-01 verification report, CITM evidence, correlation test suite |

---

## 14. Authority

| Document | Role |
|----------|------|
| [`M2-S3-Snapshot-Freeze-Design.md`](M2-S3-Snapshot-Freeze-Design.md) | Blueprint §9, INV-1, INV-7 |
| [`M2-S3-Implementation-Authorization.md`](M2-S3-Implementation-Authorization.md) | IA-001 |
| [`E-01-Implementation-Plan.md`](E-01-Implementation-Plan.md) | Phase 3 plan |
| [`ER-001-M2-S3-Blueprint-Review.md`](ER-001-M2-S3-Blueprint-Review.md) | Engineering Review |
| [`E-01-IU-3.1-Completion.md`](E-01-IU-3.1-Completion.md) | Prerequisite IU |
| [`E-01-IU-2.2-Completion.md`](E-01-IU-2.2-Completion.md) | Immutability pattern reference |

---

## Document control

| Field | Value |
|-------|-------|
| **Standard** | [`CES-010-Documentation-and-Knowledge-Engineering-Standard.md`](CES-010-Documentation-and-Knowledge-Engineering-Standard.md) |
| **Template** | [`templates/IU-Completion-Template.md`](templates/IU-Completion-Template.md) |

**Depends on:** [`20261727120000_e01_iu31_resolution_snapshot_foundation.sql`](../../supabase/migrations/20261727120000_e01_iu31_resolution_snapshot_foundation.sql) (IU-3.1)
